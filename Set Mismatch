nums = list(map(int, input().split()))

seen = set()

for x in nums:
    if x in seen:
        duplicate = x
    seen.add(x)

for i in range(1, len(nums) + 1):
    if i not in seen:
        missing = i

print(duplicate, missing)
