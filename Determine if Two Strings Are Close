def closeStrings(self, word1, word2):
  if set(word1) != set(word2):
    return False
  count1 = []
  count2 = []
  for ch in set(word1):
    count1.append(word1.count(ch))
    count2.append(word2.count(ch))
  return sorted(count1) == sorted(count2)
