class Solution:
    def differenceOfSum(self, nums: list[int]) -> int:
        sum=0
        
        a=0
        for i in range(len(nums)):
            sum+=nums[i]
            for j in str(nums[i]):
                a+=int(j)
        return sum-a
